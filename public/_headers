# Revalidate CSS/JS on every load (cheap 304s) so deploys show up immediately.
# Pages' default is max-age=14400, which paired new HTML with a 4h-stale style.css.
/*.css
  Cache-Control: no-cache
/*.js
  Cache-Control: no-cache
